<!DOCTYPE html>
<html>

<head>

    <meta charset="utf-8">
    <title>Paytm Payment</title>

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.0.0-alpha.6/css/bootstrap.min.css" />

</head>

<body>

    {{> navbar}}

    <div class="jumbotron">
        <h1 class="display-3">Paytm Incorporation Demo</h1>
        <p class="lead">Following form can be used to verify checksum.</p>
    </div>

    <div class="container">
        <div class="row">
            <form class="col-sm-8 offset-md-2" action="/verify-checksum" method="post">
                {{#if success}}
                    <div class="alert alert-success" role="alert">
                        <strong>Valid Checksum</strong> The data provided is not tempered.
                    </div>
                {{/if}}
                {{#if failed}}
                    <div class="alert alert-danger" role="alert">
                        <strong>Invalid Checksum</strong> The data provided is tempered.
                    </div>
                {{/if}}
                <div class="form-group">
                    <label for="DATA">DATA</label>
                    <textarea class="form-control" id="DATA" name="DATA" rows="18" placeholder="Paste JSON data here...">{{DATA}}</textarea>
                </div>
                <div class="form-group">
                    <label for="KEY">KEY</label>
                    <input class="form-control" id="KEY" type="text" name="KEY" value="{{KEY}}">
                </div>
                <button type="submit" class="btn btn-primary">Verify</button>
            </form>
        </div>
        <br>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.0.0-alpha.6/js/bootstrap.min.js"></script>

    <script type="text/javascript">
        $("form").submit(function(e) {
            // e.preventDefault();
            // console.log($(this).serialize());
        });
    </script>
</body>

</html>
